# Copy to .env (gitignored) and fill in. Only needed if you want AI enrichment;
# docker-compose.yml reads this file automatically.
#
#   cp .env.example .env

# API key for the OpenAI-protocol endpoint used by AI enrichment.
STASH_AI_API_KEY=
