@page "/locations"
@inject NavigationManager Nav

@* The old Locations page is superseded by the Places explorer; the route is kept so
   bookmarks and old links keep working. *@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("places", replace: true);
}
